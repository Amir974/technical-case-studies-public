# Synthetic Examples

These files are synthetic/public-safe composites derived from system structure, not real records.

They are intended to show the protocol shape:

- bounded packet;
- AI assessment over that packet;
- generic command loop;
- allowed decisions;
- explicit mutation preview and audit fields.

Do not treat these examples as operational data.
